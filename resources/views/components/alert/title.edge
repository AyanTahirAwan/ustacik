@let(titleFromSlot = await $slots.main())
@let(title = titleFromSlot.trim() ? titleFromSlot : $props.get('text'))
@let(classes = [$props.get('class')].filter(Boolean))

<h2 {{ $props.except(['text']).merge({ class: classes }).toAttrs() }}>{{{ title }}}</h2>
