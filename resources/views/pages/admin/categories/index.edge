@dashboard_layout({ title: 'Manage Categories' })
  <div class="page-placeholder">
    <h2>Categories Management — Placeholder</h2>
    <p>Categories are fixed at 8 and cannot be created or deleted. This page lists them via GET /api/admin/categories and allows editing only, via PATCH /api/admin/categories/:id -> /admin/categories/:id/edit.</p>
    <p>Categories management table will go here.</p>
  </div>
@end
