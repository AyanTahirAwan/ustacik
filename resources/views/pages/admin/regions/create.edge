@dashboard_layout({ title: 'Create Region' })
  <div class="page-placeholder">
    <h2>Create Region — Placeholder</h2>
    {{--
      TODO: API integration
      Create a region via:
        POST /api/admin/regions
      Request body:
        nameEn (string, required) — trimmed, min 2, max 120 chars, unique
        nameTr (string, required) — trimmed, min 2, max 120 chars, unique
      Responses: 201 Created | 401 | 403 | 422 | 500
    --}}
    <p>The create region form will go here.</p>
  </div>
@end

