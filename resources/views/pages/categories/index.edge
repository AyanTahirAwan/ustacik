@layout()
  <div class="hero">
    <h1>Categories</h1>
    <p>Browse all service categories.</p>
  </div>

  <div class="page-placeholder">
    <h2>Category List — Placeholder</h2>
    {{--
      TODO: API integration
      Fetch and list categories from:
        GET /api/catalog/categories
      Expected response: { data: [{ id, nameEn, nameTr, createdAt }] }
      Each item should link to /categories/:id (Category Details).
    --}}
    <p>No categories loaded yet. This page will list service categories.</p>
  </div>
@end

