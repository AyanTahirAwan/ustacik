@layout()
  <div class="hero">
    <h1>Craftsman Listings</h1>
    <p>Browse craftsmen and their service listings.</p>
  </div>

  <div class="page-placeholder">
    <h2>Craftsman Listings — Placeholder</h2>
    {{--
      TODO: API integration
      List craftsmen and their service prices from:
        GET /api/catalog/prices (active entries, preloads craftsman, subService, region)
        GET /api/search/services (searchable entries)
      Expected response: { servicePriceCatalogs: [...] } or { data: [...] }
      Group/display by craftsman (businessName, trustLevel, totalJobs).
    --}}
    <p>No listings loaded yet. This page will show craftsman service listings.</p>
  </div>
@end

