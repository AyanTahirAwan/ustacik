@layout()
  <section aria-labelledby="craftsmen-heading">
    <div class="section-header">
      <div>
        <h2 id="craftsmen-heading" class="section-heading">{{ t('craftsmen.local_pros') }}</h2>
        <p class="section-subheading">{{ t('craftsmen.sub') }}</p>
      </div>
      <a class="btn-back" href="/search">{{ t('craftsmen.filter_services') }}</a>
    </div>
    <div id="craftsmen-list" class="craftsmen-grid" aria-live="polite">
      <div class="empty-state">
        <p>{{ t('btn.loading_craftsmen') }}</p>
      </div>
    </div>
  </section>
@end
